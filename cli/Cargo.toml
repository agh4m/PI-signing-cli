[package]
name = "cli"
version = "0.1.0"
edition = "2021"

[dependencies]
clap = { version = "4.5.2", features = ["derive"] }
dotenv_codegen = "0.15.0"
dotenv = "0.15.0"
tokio = { version = "1.37.0", features = ["full"] }
disa_lib = { path = "../sig_lib" }
